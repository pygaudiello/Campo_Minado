import 'package:flutter/material.dart';

class CampoWidget extends StatelessWidget {
  const CampoWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
